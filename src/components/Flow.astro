---
const {  anchor = "top", viewBox } = Astro.props;
---

<svg class:list={["fill-white", "dark:fill-gray-900", anchor]} {viewBox}>
<slot/>
<svg>

<style>
svg {
  animation: flow 10s linear infinite;
}
.top {
  transform-origin: top left;
}
.bottom {
  transform-origin: bottom left;
}
@keyframes flow {

0%{
  transform: scale(1, 0);
}

20%{
  transform: scale(1, 0.7);
}

40%{
  transform: scale(1, 0.3);
}

70%{
  transform: scale(1, 1);
}

100% {
  transform: scale(1, 0);
}
}
</style>
