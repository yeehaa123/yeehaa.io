---
const {  anchor = "top", viewBox, class:className} = Astro.props;
---

<svg class:list={["fill-white", "dark:fill-black", "relative", "-bottom-1", anchor, className]} {viewBox}>
<slot/>
<svg>

<style>
svg {
  animation: flow 10s linear infinite;
}
.top {
  transform-origin: top left;
}
.bottom {
  transform-origin: bottom center;
}
@keyframes flow {

0%{
  transform: scale(1, 0);
}

20%{
  transform: scale(1, 0.7);
}

40%{
  transform: scale(1, 0.3);
}

70%{
  transform: scale(1, 1);
}

100% {
  transform: scale(1, 0);
}
}
</style>
